using UnityEngine;

public class MyStat_Ctrl : MonoBehaviour
{
    public MyStat_Obj Menu_Obj;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
