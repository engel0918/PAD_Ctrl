using UnityEngine;

public class Exit_Ctrl : MonoBehaviour
{
    public Exit_Obj Menu_Obj;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
